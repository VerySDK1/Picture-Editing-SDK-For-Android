<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        style="@style/menu_title"
        android:text="@string/app_camera" />

    <View style="@style/menu_line" />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">

        <!--相机拍照-->
        <LinearLayout
            android:id="@+id/btnCameraShot"
            style="@style/menu_item">

            <TextView
                style="@style/menu_item_title"
                android:text="@string/app_take_photo" />

            <ImageView style="@style/menu_arrow" />

        </LinearLayout>

        <View
            style="@style/menu_line_sub"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/btnCameraRecord"
            style="@style/menu_item"
            android:visibility="gone">

            <TextView
                style="@style/menu_item_title"
                android:text="(2)  拍摄" />

            <ImageView style="@style/menu_arrow" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>